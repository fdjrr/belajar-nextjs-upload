/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: "https",
                hostname: "fopxzxzddfn3odi0.public.blob.vercel-storage.com",
            },
        ],
    },
};

export default nextConfig;
